# Bioinformatics Code MCP — Python Dependencies
#
# Install all:  pip install -r requirements.txt
# Or use:       pip install -e ".[dev]"
#
# Pinned from a known-working environment (Python 3.11).

# ── Core dependencies ─────────────────────────────────────────────────
mcp>=1.0.0
httpx>=0.27.0
biopython>=1.84
pydantic>=2.0
restrictedpython>=7.0

# ── Playground (interactive web UI) ───────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0
jinja2>=3.1.0

# ── Development & testing ─────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
ruff>=0.8.0
mypy>=1.13
