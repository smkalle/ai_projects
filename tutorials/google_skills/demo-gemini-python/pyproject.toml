[project]
name = "demo-gemini-python"
version = "0.1.0"
description = "Gemini API Python demo CLI"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=0.8",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
    "pytest>=8.0",
]

[project.scripts]
demo-gemini-python = "main:main"
