# Sample Files

Place sample files here to test multimodal capabilities:

- **Images**: `.jpg`, `.png`, `.webp` — for vision demos
- **Audio**: `.mp3`, `.wav`, `.ogg`, `.flac` — for audio transcription/summary

The CLI and Streamlit apps accept any file path, but this directory is a convenient place to keep test files.
